<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_cadastro"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.com.gustavo.firebaseapp.activity.CadastroActivity">

    <TextView
        android:id="@+id/txCadastrese"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:text="Cadastre-se"/>

    <EditText
        android:id="@+id/cpCadNome"
        android:layout_below="@+id/txCadastrese"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/textoCixa"
        android:hint="Nome"/>

    <EditText
        android:id="@+id/cpCadEmail"
        android:layout_below="@+id/cpCadNome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/textoCixa"
        android:hint="Email"/>

    <EditText
        android:id="@+id/cpCadSenha"
        android:layout_below="@+id/cpCadEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:theme="@style/textoCixa"
        android:hint="Password"/>

    <Button
        android:id="@+id/btnCadCadastrar"
        android:layout_below="@+id/cpCadSenha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cadastrar"
        android:theme="@style/botaoPadrao"/>

</RelativeLayout>
